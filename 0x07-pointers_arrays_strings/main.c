#include <stdio.h>
#include <string.h>
#include "main.h"

/**
	* main - Entry point
	* Description: trial for white spaces
	* Return: returns (0) Always success
*/

int main(void)
{

	char *j;

	j = "Hello, World!";
	printf("j: %s\n", j);

	return (strlen(j));
}
