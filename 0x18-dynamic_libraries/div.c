#include "main.h"
#include <stdio.h>

/**
 * div- Function to divide two numbers
 *
 * @a: integer a
 * @b: integer b
 * @result: pointer to result
 *
 * Return: the division of the two numbers
*/


int div(int a, int b)
{
	return (a / b);
}
